# recuperatorio-mod4
# Nota

correr el comando en la raiz del proyecto para inicializar la base de datos usada:

docker compose -f .\docker-compose\docker-compose.yml up -d

En el postman cuando hace login, el token queda guardado en una variable global para ser usado en los endpoint recetas.
